import type { Context } from '@deepseek-ai/cordis';
export declare const name = "qwen-voice";
/** Client-only entry; the host fiber exists so the bundle can join a profile. */
export declare function apply(_ctx: Context): void;
